import React from 'react';

export const InfoBoard = () => {
    // NOTE VARIABLES AND STATES\

    const donorTabs = [
        "Donor Accounts", "Account Details", 
        "Edit Settings", "Payments"
    ]

    const adminTabs = [
        "Account Details", "Edit Settings", "Active Listings",
        "Archive", "Payments"
    ]

    // NOTE FUNCTIONS

  
    // NOTE RETURNS
    return (
        <>
        </>
    )
}

export default InfoBoard;
